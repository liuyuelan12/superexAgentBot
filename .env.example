# SuperEx Agent Bot reads these from the repo-root .env (../../.env).
# Do not commit real values.

BOT_TOKEN=                 # Telegram bot token for SuperExAgentBot
GROQ_API_KEY=              # Primary LLM
DEEPSEEK_API_KEY=          # Fallback LLM
